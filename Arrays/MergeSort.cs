class MergeSort
{
    static void swap(int[] arr, int i, int j)
    {
        if (i < 0 || j < 0 || i >= att.Length || j >= arr.Length)
            throw new ArgumentOutOfRangeException('Index out of range!');
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;

    }




    static void MergeSort(int[] arr1, int[] arr2)
    {
        int[] result = new int[]();
        
    }
}